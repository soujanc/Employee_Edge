export const TextButtons = ({ label, children }) => {
    return (     
      <a
      className="text-blue-600 hover:text-blue-700 hover:underline hover:underline-offset-4">
        {label}
        </a> 
 )
}

